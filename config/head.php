<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Inertia Page Prop
    |--------------------------------------------------------------------------
    |
    | When Inertia is installed, the rendered head elements are shared with
    | every Inertia page under this prop. You may change it if your app
    | already uses the "head" prop for something else (e.g. "_head").
    |
    */

    'inertia' => [
        'prop' => 'head',
    ],

];
